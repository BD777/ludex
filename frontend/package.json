{
  "name": "ludex-frontend",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BD777/ludex.git",
    "directory": "frontend"
  },
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true, maxRetries: 10, retryDelay: 100 })\" && vue-tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^8.1.2",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vue-tsc": "^3.1.5"
  }
}
